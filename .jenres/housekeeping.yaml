duties:
  bunnies:
    cron: "* * * * *"
    prompt: in the folder usecase_prj add an initial messagge which describes the content of the file.

  logging:
    cron: "* * * * *"
    prompt: |
      in the file {file_path} add logging lines of appropriated level where needed.
